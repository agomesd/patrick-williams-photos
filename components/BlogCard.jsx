import styles from '../styles/BlogCard.module.css';

const BlogCard = ({ data }) => {
    return (
        <div className={styles.container}>
            <h1>Blog</h1>
        </div>
    )
}

export default BlogCard;